// WARNING: This file was generated by PrimeCodeGen. DO NOT EDIT.

package io.github.primelib.jira4j.rest.v2.models;

import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;
import com.fasterxml.jackson.annotation.JsonTypeName;
import java.util.Objects;
import java.util.function.Consumer;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;

/**
 * UiModificationContextDetails
 * <p>
 * The details of a UI modification's context, which define where to activate the UI modification.
 *
 */
@JsonTypeName("UiModificationContextDetails")
@JsonPropertyOrder({
    "id",
    "isAvailable",
    "issueTypeId",
    "projectId",
    "viewType"
})
@Generated(value = "io.github.primelib.primecodegen")
public class UiModificationContextDetails {

    /**
     * The ID of the UI modification context.
     */
    @JsonProperty("id")
    protected String id;

    /**
     * Whether a context is available. For example, when a project is deleted the context becomes unavailable.
     */
    @JsonProperty("isAvailable")
    protected Boolean isAvailable;

    /**
     * The issue type ID of the context. Null is treated as a wildcard, meaning the UI modification will be applied to all issue types. Each UI modification context can have a maximum of one wildcard.
     */
    @JsonProperty("issueTypeId")
    protected String issueTypeId;

    /**
     * The project ID of the context. Null is treated as a wildcard, meaning the UI modification will be applied to all projects. Each UI modification context can have a maximum of one wildcard.
     */
    @JsonProperty("projectId")
    protected String projectId;

    /**
     * The view type of the context. Only `GIC`(Global Issue Create), `IssueView` and `IssueTransition` are supported. Null is treated as a wildcard, meaning the UI modification will be applied to all view types. Each UI modification context can have a maximum of one wildcard.
     */
    @JsonProperty("viewType")
    protected String viewType;

    /**
     * Constructs a validated instance of {@link UiModificationContextDetails}.
     *
     * @param spec the specification to process
     */
    public UiModificationContextDetails(Consumer<UiModificationContextDetails> spec) {
        super();
        spec.accept(this);
    }

    /**
     * Protected no-args constructor for use by serialization frameworks.
     */
    @ApiStatus.Internal
    protected UiModificationContextDetails() {
    }

    /**
     * Constructs a validated instance of {@link UiModificationContextDetails}.
     * <p>
     * NOTE: This constructor is not considered stable and may change if the model is updated. Consider using {@link #UiModificationContextDetails(Consumer)} instead.
     * @param id The ID of the UI modification context.
     * @param isAvailable Whether a context is available. For example, when a project is deleted the context becomes unavailable.
     * @param issueTypeId The issue type ID of the context. Null is treated as a wildcard, meaning the UI modification will be applied to all issue types. Each UI modification context can have a maximum of one wildcard.
     * @param projectId The project ID of the context. Null is treated as a wildcard, meaning the UI modification will be applied to all projects. Each UI modification context can have a maximum of one wildcard.
     * @param viewType The view type of the context. Only `GIC`(Global Issue Create), `IssueView` and `IssueTransition` are supported. Null is treated as a wildcard, meaning the UI modification will be applied to all view types. Each UI modification context can have a maximum of one wildcard.
     */
    @ApiStatus.Internal
    public UiModificationContextDetails(String id, Boolean isAvailable, String issueTypeId, String projectId, String viewType) {
        this.id = id;
        this.isAvailable = isAvailable;
        this.issueTypeId = issueTypeId;
        this.projectId = projectId;
        this.viewType = viewType;
    }


    /**
     * Fluent getter for id.
     * <p>
     * The ID of the UI modification context.
     *
     * @return id
     */
    public String id() {
        return this.id;
    }

    /**
     * Fluent setter for id.
     * <p>
     * The ID of the UI modification context.
     *
     * @param id id
     * @return this
     */
    public UiModificationContextDetails id(String id) {
        this.id = id;
        return this;
    }

    /**
     * Gets the value of id.
     * <p>
     * The ID of the UI modification context.
     *
     * @return id
     */
    @JsonProperty("id")
    public String getId() {
        return this.id;
    }

    /**
     * Sets the value of id.
     * <p>
     * The ID of the UI modification context.
     *
     * @param id id
     */
    public void setId(String id) {
        this.id = id;
    }
    /**
     * Fluent getter for isAvailable.
     * <p>
     * Whether a context is available. For example, when a project is deleted the context becomes unavailable.
     *
     * @return isAvailable
     */
    public Boolean isAvailable() {
        return this.isAvailable;
    }

    /**
     * Fluent setter for isAvailable.
     * <p>
     * Whether a context is available. For example, when a project is deleted the context becomes unavailable.
     *
     * @param isAvailable isAvailable
     * @return this
     */
    public UiModificationContextDetails isAvailable(Boolean isAvailable) {
        this.isAvailable = isAvailable;
        return this;
    }

    /**
     * Gets the value of isAvailable.
     * <p>
     * Whether a context is available. For example, when a project is deleted the context becomes unavailable.
     *
     * @return isAvailable
     */
    @JsonProperty("isAvailable")
    public Boolean getIsAvailable() {
        return this.isAvailable;
    }

    /**
     * Sets the value of isAvailable.
     * <p>
     * Whether a context is available. For example, when a project is deleted the context becomes unavailable.
     *
     * @param isAvailable isAvailable
     */
    public void setIsAvailable(Boolean isAvailable) {
        this.isAvailable = isAvailable;
    }
    /**
     * Fluent getter for issueTypeId.
     * <p>
     * The issue type ID of the context. Null is treated as a wildcard, meaning the UI modification will be applied to all issue types. Each UI modification context can have a maximum of one wildcard.
     *
     * @return issueTypeId
     */
    public String issueTypeId() {
        return this.issueTypeId;
    }

    /**
     * Fluent setter for issueTypeId.
     * <p>
     * The issue type ID of the context. Null is treated as a wildcard, meaning the UI modification will be applied to all issue types. Each UI modification context can have a maximum of one wildcard.
     *
     * @param issueTypeId issueTypeId
     * @return this
     */
    public UiModificationContextDetails issueTypeId(String issueTypeId) {
        this.issueTypeId = issueTypeId;
        return this;
    }

    /**
     * Gets the value of issueTypeId.
     * <p>
     * The issue type ID of the context. Null is treated as a wildcard, meaning the UI modification will be applied to all issue types. Each UI modification context can have a maximum of one wildcard.
     *
     * @return issueTypeId
     */
    @JsonProperty("issueTypeId")
    public String getIssueTypeId() {
        return this.issueTypeId;
    }

    /**
     * Sets the value of issueTypeId.
     * <p>
     * The issue type ID of the context. Null is treated as a wildcard, meaning the UI modification will be applied to all issue types. Each UI modification context can have a maximum of one wildcard.
     *
     * @param issueTypeId issueTypeId
     */
    public void setIssueTypeId(String issueTypeId) {
        this.issueTypeId = issueTypeId;
    }
    /**
     * Fluent getter for projectId.
     * <p>
     * The project ID of the context. Null is treated as a wildcard, meaning the UI modification will be applied to all projects. Each UI modification context can have a maximum of one wildcard.
     *
     * @return projectId
     */
    public String projectId() {
        return this.projectId;
    }

    /**
     * Fluent setter for projectId.
     * <p>
     * The project ID of the context. Null is treated as a wildcard, meaning the UI modification will be applied to all projects. Each UI modification context can have a maximum of one wildcard.
     *
     * @param projectId projectId
     * @return this
     */
    public UiModificationContextDetails projectId(String projectId) {
        this.projectId = projectId;
        return this;
    }

    /**
     * Gets the value of projectId.
     * <p>
     * The project ID of the context. Null is treated as a wildcard, meaning the UI modification will be applied to all projects. Each UI modification context can have a maximum of one wildcard.
     *
     * @return projectId
     */
    @JsonProperty("projectId")
    public String getProjectId() {
        return this.projectId;
    }

    /**
     * Sets the value of projectId.
     * <p>
     * The project ID of the context. Null is treated as a wildcard, meaning the UI modification will be applied to all projects. Each UI modification context can have a maximum of one wildcard.
     *
     * @param projectId projectId
     */
    public void setProjectId(String projectId) {
        this.projectId = projectId;
    }
    /**
     * Fluent getter for viewType.
     * <p>
     * The view type of the context. Only `GIC`(Global Issue Create), `IssueView` and `IssueTransition` are supported. Null is treated as a wildcard, meaning the UI modification will be applied to all view types. Each UI modification context can have a maximum of one wildcard.
     *
     * @return viewType
     */
    public String viewType() {
        return this.viewType;
    }

    /**
     * Fluent setter for viewType.
     * <p>
     * The view type of the context. Only `GIC`(Global Issue Create), `IssueView` and `IssueTransition` are supported. Null is treated as a wildcard, meaning the UI modification will be applied to all view types. Each UI modification context can have a maximum of one wildcard.
     *
     * @param viewType viewType
     * @return this
     */
    public UiModificationContextDetails viewType(String viewType) {
        this.viewType = viewType;
        return this;
    }

    /**
     * Gets the value of viewType.
     * <p>
     * The view type of the context. Only `GIC`(Global Issue Create), `IssueView` and `IssueTransition` are supported. Null is treated as a wildcard, meaning the UI modification will be applied to all view types. Each UI modification context can have a maximum of one wildcard.
     *
     * @return viewType
     */
    @JsonProperty("viewType")
    public String getViewType() {
        return this.viewType;
    }

    /**
     * Sets the value of viewType.
     * <p>
     * The view type of the context. Only `GIC`(Global Issue Create), `IssueView` and `IssueTransition` are supported. Null is treated as a wildcard, meaning the UI modification will be applied to all view types. Each UI modification context can have a maximum of one wildcard.
     *
     * @param viewType viewType
     */
    public void setViewType(String viewType) {
        this.viewType = viewType;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        UiModificationContextDetails that = (UiModificationContextDetails) o;
        return
            Objects.equals(this.id, that.id) &&
            Objects.equals(this.isAvailable, that.isAvailable) &&
            Objects.equals(this.issueTypeId, that.issueTypeId) &&
            Objects.equals(this.projectId, that.projectId) &&
            Objects.equals(this.viewType, that.viewType);
    }

    @Override
    public int hashCode() {
        return Objects.hash(
            this.id, 
            this.isAvailable, 
            this.issueTypeId, 
            this.projectId, 
            this.viewType
        );
    }

    @Override
    public String toString() {
        return "UiModificationContextDetails{" +
            "id=" + id + ", " + 
            "isAvailable=" + isAvailable + ", " + 
            "issueTypeId=" + issueTypeId + ", " + 
            "projectId=" + projectId + ", " + 
            "viewType=" + viewType +
            "}";
    }
}
