// WARNING: This file was generated by PrimeCodeGen. DO NOT EDIT.

package io.github.primelib.jira4j.rest.v2.models;

import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;
import com.fasterxml.jackson.annotation.JsonTypeName;
import java.util.List;
import java.util.Objects;
import java.util.function.Consumer;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;

/**
 * CreateWorkflowDetails
 * <p>
 * The details of a workflow.
 *
 */
@JsonTypeName("CreateWorkflowDetails")
@JsonPropertyOrder({
    "description",
    "name",
    "statuses",
    "transitions"
})
@Generated(value = "io.github.primelib.primecodegen")
public class CreateWorkflowDetails {

    /**
     * The description of the workflow. The maximum length is 1000 characters.
     */
    @JsonProperty("description")
    protected String description;

    /**
     * The name of the workflow. The name must be unique. The maximum length is 255 characters. Characters can be separated by a whitespace but the name cannot start or end with a whitespace.
     */
    @JsonProperty("name")
    protected String name;

    /**
     * The statuses of the workflow. Any status that does not include a transition is added to the workflow without a transition.
     */
    @JsonProperty("statuses")
    protected List<CreateWorkflowStatusDetails> statuses;

    /**
     * The transitions of the workflow. For the request to be valid, these transitions must:

 *  include one *initial* transition.
 *  not use the same name for a *global* and *directed* transition.
 *  have a unique name for each *global* transition.
 *  have a unique 'to' status for each *global* transition.
 *  have unique names for each transition from a status.
 *  not have a 'from' status on *initial* and *global* transitions.
 *  have a 'from' status on *directed* transitions.

All the transition statuses must be included in `statuses`.
     */
    @JsonProperty("transitions")
    protected List<CreateWorkflowTransitionDetails> transitions;

    /**
     * Constructs a validated instance of {@link CreateWorkflowDetails}.
     *
     * @param spec the specification to process
     */
    public CreateWorkflowDetails(Consumer<CreateWorkflowDetails> spec) {
        super();
        spec.accept(this);
    }

    /**
     * Protected no-args constructor for use by serialization frameworks.
     */
    @ApiStatus.Internal
    protected CreateWorkflowDetails() {
    }

    /**
     * Constructs a validated instance of {@link CreateWorkflowDetails}.
     * <p>
     * NOTE: This constructor is not considered stable and may change if the model is updated. Consider using {@link #CreateWorkflowDetails(Consumer)} instead.
     * @param description The description of the workflow. The maximum length is 1000 characters.
     * @param name The name of the workflow. The name must be unique. The maximum length is 255 characters. Characters can be separated by a whitespace but the name cannot start or end with a whitespace.
     * @param statuses The statuses of the workflow. Any status that does not include a transition is added to the workflow without a transition.
     * @param transitions The transitions of the workflow. For the request to be valid, these transitions must:

 *  include one *initial* transition.
 *  not use the same name for a *global* and *directed* transition.
 *  have a unique name for each *global* transition.
 *  have a unique 'to' status for each *global* transition.
 *  have unique names for each transition from a status.
 *  not have a 'from' status on *initial* and *global* transitions.
 *  have a 'from' status on *directed* transitions.

All the transition statuses must be included in `statuses`.
     */
    @ApiStatus.Internal
    public CreateWorkflowDetails(String description, String name, List<CreateWorkflowStatusDetails> statuses, List<CreateWorkflowTransitionDetails> transitions) {
        this.description = description;
        this.name = name;
        this.statuses = statuses;
        this.transitions = transitions;
    }


    /**
     * Fluent getter for description.
     * <p>
     * The description of the workflow. The maximum length is 1000 characters.
     *
     * @return description
     */
    public String description() {
        return this.description;
    }

    /**
     * Fluent setter for description.
     * <p>
     * The description of the workflow. The maximum length is 1000 characters.
     *
     * @param description description
     * @return this
     */
    public CreateWorkflowDetails description(String description) {
        this.description = description;
        return this;
    }

    /**
     * Gets the value of description.
     * <p>
     * The description of the workflow. The maximum length is 1000 characters.
     *
     * @return description
     */
    @JsonProperty("description")
    public String getDescription() {
        return this.description;
    }

    /**
     * Sets the value of description.
     * <p>
     * The description of the workflow. The maximum length is 1000 characters.
     *
     * @param description description
     */
    public void setDescription(String description) {
        this.description = description;
    }
    /**
     * Fluent getter for name.
     * <p>
     * The name of the workflow. The name must be unique. The maximum length is 255 characters. Characters can be separated by a whitespace but the name cannot start or end with a whitespace.
     *
     * @return name
     */
    public String name() {
        return this.name;
    }

    /**
     * Fluent setter for name.
     * <p>
     * The name of the workflow. The name must be unique. The maximum length is 255 characters. Characters can be separated by a whitespace but the name cannot start or end with a whitespace.
     *
     * @param name name
     * @return this
     */
    public CreateWorkflowDetails name(String name) {
        this.name = name;
        return this;
    }

    /**
     * Gets the value of name.
     * <p>
     * The name of the workflow. The name must be unique. The maximum length is 255 characters. Characters can be separated by a whitespace but the name cannot start or end with a whitespace.
     *
     * @return name
     */
    @JsonProperty("name")
    public String getName() {
        return this.name;
    }

    /**
     * Sets the value of name.
     * <p>
     * The name of the workflow. The name must be unique. The maximum length is 255 characters. Characters can be separated by a whitespace but the name cannot start or end with a whitespace.
     *
     * @param name name
     */
    public void setName(String name) {
        this.name = name;
    }
    /**
     * Fluent getter for statuses.
     * <p>
     * The statuses of the workflow. Any status that does not include a transition is added to the workflow without a transition.
     *
     * @return statuses
     */
    public List<CreateWorkflowStatusDetails> statuses() {
        return this.statuses;
    }

    /**
     * Fluent setter for statuses.
     * <p>
     * The statuses of the workflow. Any status that does not include a transition is added to the workflow without a transition.
     *
     * @param statuses statuses
     * @return this
     */
    public CreateWorkflowDetails statuses(List<CreateWorkflowStatusDetails> statuses) {
        this.statuses = statuses;
        return this;
    }

    /**
     * Gets the value of statuses.
     * <p>
     * The statuses of the workflow. Any status that does not include a transition is added to the workflow without a transition.
     *
     * @return statuses
     */
    @JsonProperty("statuses")
    public List<CreateWorkflowStatusDetails> getStatuses() {
        return this.statuses;
    }

    /**
     * Sets the value of statuses.
     * <p>
     * The statuses of the workflow. Any status that does not include a transition is added to the workflow without a transition.
     *
     * @param statuses statuses
     */
    public void setStatuses(List<CreateWorkflowStatusDetails> statuses) {
        this.statuses = statuses;
    }
    /**
     * Fluent getter for transitions.
     * <p>
     * The transitions of the workflow. For the request to be valid, these transitions must:
     * 
     *  *  include one *initial* transition.
     *  *  not use the same name for a *global* and *directed* transition.
     *  *  have a unique name for each *global* transition.
     *  *  have a unique 'to' status for each *global* transition.
     *  *  have unique names for each transition from a status.
     *  *  not have a 'from' status on *initial* and *global* transitions.
     *  *  have a 'from' status on *directed* transitions.
     * 
     * All the transition statuses must be included in `statuses`.
     *
     * @return transitions
     */
    public List<CreateWorkflowTransitionDetails> transitions() {
        return this.transitions;
    }

    /**
     * Fluent setter for transitions.
     * <p>
     * The transitions of the workflow. For the request to be valid, these transitions must:
     * 
     *  *  include one *initial* transition.
     *  *  not use the same name for a *global* and *directed* transition.
     *  *  have a unique name for each *global* transition.
     *  *  have a unique 'to' status for each *global* transition.
     *  *  have unique names for each transition from a status.
     *  *  not have a 'from' status on *initial* and *global* transitions.
     *  *  have a 'from' status on *directed* transitions.
     * 
     * All the transition statuses must be included in `statuses`.
     *
     * @param transitions transitions
     * @return this
     */
    public CreateWorkflowDetails transitions(List<CreateWorkflowTransitionDetails> transitions) {
        this.transitions = transitions;
        return this;
    }

    /**
     * Gets the value of transitions.
     * <p>
     * The transitions of the workflow. For the request to be valid, these transitions must:
     * 
     *  *  include one *initial* transition.
     *  *  not use the same name for a *global* and *directed* transition.
     *  *  have a unique name for each *global* transition.
     *  *  have a unique 'to' status for each *global* transition.
     *  *  have unique names for each transition from a status.
     *  *  not have a 'from' status on *initial* and *global* transitions.
     *  *  have a 'from' status on *directed* transitions.
     * 
     * All the transition statuses must be included in `statuses`.
     *
     * @return transitions
     */
    @JsonProperty("transitions")
    public List<CreateWorkflowTransitionDetails> getTransitions() {
        return this.transitions;
    }

    /**
     * Sets the value of transitions.
     * <p>
     * The transitions of the workflow. For the request to be valid, these transitions must:
     * 
     *  *  include one *initial* transition.
     *  *  not use the same name for a *global* and *directed* transition.
     *  *  have a unique name for each *global* transition.
     *  *  have a unique 'to' status for each *global* transition.
     *  *  have unique names for each transition from a status.
     *  *  not have a 'from' status on *initial* and *global* transitions.
     *  *  have a 'from' status on *directed* transitions.
     * 
     * All the transition statuses must be included in `statuses`.
     *
     * @param transitions transitions
     */
    public void setTransitions(List<CreateWorkflowTransitionDetails> transitions) {
        this.transitions = transitions;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        CreateWorkflowDetails that = (CreateWorkflowDetails) o;
        return
            Objects.equals(this.description, that.description) &&
            Objects.equals(this.name, that.name) &&
            Objects.equals(this.statuses, that.statuses) &&
            Objects.equals(this.transitions, that.transitions);
    }

    @Override
    public int hashCode() {
        return Objects.hash(
            this.description, 
            this.name, 
            this.statuses, 
            this.transitions
        );
    }

    @Override
    public String toString() {
        return "CreateWorkflowDetails{" +
            "description=" + description + ", " + 
            "name=" + name + ", " + 
            "statuses=" + statuses + ", " + 
            "transitions=" + transitions +
            "}";
    }
}
