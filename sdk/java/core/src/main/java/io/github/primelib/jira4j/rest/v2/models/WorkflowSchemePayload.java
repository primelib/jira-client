// WARNING: This file was generated by PrimeCodeGen. DO NOT EDIT.

package io.github.primelib.jira4j.rest.v2.models;

import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;
import com.fasterxml.jackson.annotation.JsonTypeName;
import java.util.Objects;
import java.util.function.Consumer;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;

/**
 * WorkflowSchemePayload
 * <p>
 * The payload for creating a workflow scheme. See https://www.atlassian.com/software/jira/guides/workflows/overview\#what-is-a-jira-workflow-scheme
 *
 */
@JsonTypeName("WorkflowSchemePayload")
@JsonPropertyOrder({
    "defaultWorkflow",
    "description",
    "explicitMappings",
    "name",
    "pcri"
})
@Generated(value = "io.github.primelib.primecodegen")
public class WorkflowSchemePayload {

    /**
     * Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation
     */
    @JsonProperty("defaultWorkflow")
    protected ProjectCreateResourceIDEntifier defaultWorkflow;

    /**
     * The description of the workflow scheme
     */
    @JsonProperty("description")
    protected String description;

    /**
     * Association between issuetypes and workflows
     */
    @JsonProperty("explicitMappings")
    protected WorkflowSchemePayloadExplicitMappings explicitMappings;

    /**
     * The name of the workflow scheme
     */
    @JsonProperty("name")
    protected String name;

    /**
     * Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation
     */
    @JsonProperty("pcri")
    protected ProjectCreateResourceIDEntifier pcri;

    /**
     * Constructs a validated instance of {@link WorkflowSchemePayload}.
     *
     * @param spec the specification to process
     */
    public WorkflowSchemePayload(Consumer<WorkflowSchemePayload> spec) {
        super();
        spec.accept(this);
    }

    /**
     * Protected no-args constructor for use by serialization frameworks.
     */
    @ApiStatus.Internal
    protected WorkflowSchemePayload() {
    }

    /**
     * Constructs a validated instance of {@link WorkflowSchemePayload}.
     * <p>
     * NOTE: This constructor is not considered stable and may change if the model is updated. Consider using {@link #WorkflowSchemePayload(Consumer)} instead.
     * @param defaultWorkflow Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation
     * @param description The description of the workflow scheme
     * @param explicitMappings Association between issuetypes and workflows
     * @param name The name of the workflow scheme
     * @param pcri Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation
     */
    @ApiStatus.Internal
    public WorkflowSchemePayload(ProjectCreateResourceIDEntifier defaultWorkflow, String description, WorkflowSchemePayloadExplicitMappings explicitMappings, String name, ProjectCreateResourceIDEntifier pcri) {
        this.defaultWorkflow = defaultWorkflow;
        this.description = description;
        this.explicitMappings = explicitMappings;
        this.name = name;
        this.pcri = pcri;
    }


    /**
     * Fluent getter for defaultWorkflow.
     * <p>
     * Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation
     *
     * @return defaultWorkflow
     */
    public ProjectCreateResourceIDEntifier defaultWorkflow() {
        return this.defaultWorkflow;
    }

    /**
     * Fluent setter for defaultWorkflow.
     * <p>
     * Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation
     *
     * @param defaultWorkflow defaultWorkflow
     * @return this
     */
    public WorkflowSchemePayload defaultWorkflow(ProjectCreateResourceIDEntifier defaultWorkflow) {
        this.defaultWorkflow = defaultWorkflow;
        return this;
    }

    /**
     * Gets the value of defaultWorkflow.
     * <p>
     * Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation
     *
     * @return defaultWorkflow
     */
    @JsonProperty("defaultWorkflow")
    public ProjectCreateResourceIDEntifier getDefaultWorkflow() {
        return this.defaultWorkflow;
    }

    /**
     * Sets the value of defaultWorkflow.
     * <p>
     * Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation
     *
     * @param defaultWorkflow defaultWorkflow
     */
    public void setDefaultWorkflow(ProjectCreateResourceIDEntifier defaultWorkflow) {
        this.defaultWorkflow = defaultWorkflow;
    }
    /**
     * Fluent getter for description.
     * <p>
     * The description of the workflow scheme
     *
     * @return description
     */
    public String description() {
        return this.description;
    }

    /**
     * Fluent setter for description.
     * <p>
     * The description of the workflow scheme
     *
     * @param description description
     * @return this
     */
    public WorkflowSchemePayload description(String description) {
        this.description = description;
        return this;
    }

    /**
     * Gets the value of description.
     * <p>
     * The description of the workflow scheme
     *
     * @return description
     */
    @JsonProperty("description")
    public String getDescription() {
        return this.description;
    }

    /**
     * Sets the value of description.
     * <p>
     * The description of the workflow scheme
     *
     * @param description description
     */
    public void setDescription(String description) {
        this.description = description;
    }
    /**
     * Fluent getter for explicitMappings.
     * <p>
     * Association between issuetypes and workflows
     *
     * @return explicitMappings
     */
    public WorkflowSchemePayloadExplicitMappings explicitMappings() {
        return this.explicitMappings;
    }

    /**
     * Fluent setter for explicitMappings.
     * <p>
     * Association between issuetypes and workflows
     *
     * @param explicitMappings explicitMappings
     * @return this
     */
    public WorkflowSchemePayload explicitMappings(WorkflowSchemePayloadExplicitMappings explicitMappings) {
        this.explicitMappings = explicitMappings;
        return this;
    }

    /**
     * Gets the value of explicitMappings.
     * <p>
     * Association between issuetypes and workflows
     *
     * @return explicitMappings
     */
    @JsonProperty("explicitMappings")
    public WorkflowSchemePayloadExplicitMappings getExplicitMappings() {
        return this.explicitMappings;
    }

    /**
     * Sets the value of explicitMappings.
     * <p>
     * Association between issuetypes and workflows
     *
     * @param explicitMappings explicitMappings
     */
    public void setExplicitMappings(WorkflowSchemePayloadExplicitMappings explicitMappings) {
        this.explicitMappings = explicitMappings;
    }
    /**
     * Fluent getter for name.
     * <p>
     * The name of the workflow scheme
     *
     * @return name
     */
    public String name() {
        return this.name;
    }

    /**
     * Fluent setter for name.
     * <p>
     * The name of the workflow scheme
     *
     * @param name name
     * @return this
     */
    public WorkflowSchemePayload name(String name) {
        this.name = name;
        return this;
    }

    /**
     * Gets the value of name.
     * <p>
     * The name of the workflow scheme
     *
     * @return name
     */
    @JsonProperty("name")
    public String getName() {
        return this.name;
    }

    /**
     * Sets the value of name.
     * <p>
     * The name of the workflow scheme
     *
     * @param name name
     */
    public void setName(String name) {
        this.name = name;
    }
    /**
     * Fluent getter for pcri.
     * <p>
     * Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation
     *
     * @return pcri
     */
    public ProjectCreateResourceIDEntifier pcri() {
        return this.pcri;
    }

    /**
     * Fluent setter for pcri.
     * <p>
     * Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation
     *
     * @param pcri pcri
     * @return this
     */
    public WorkflowSchemePayload pcri(ProjectCreateResourceIDEntifier pcri) {
        this.pcri = pcri;
        return this;
    }

    /**
     * Gets the value of pcri.
     * <p>
     * Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation
     *
     * @return pcri
     */
    @JsonProperty("pcri")
    public ProjectCreateResourceIDEntifier getPcri() {
        return this.pcri;
    }

    /**
     * Sets the value of pcri.
     * <p>
     * Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation
     *
     * @param pcri pcri
     */
    public void setPcri(ProjectCreateResourceIDEntifier pcri) {
        this.pcri = pcri;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        WorkflowSchemePayload that = (WorkflowSchemePayload) o;
        return
            Objects.equals(this.defaultWorkflow, that.defaultWorkflow) &&
            Objects.equals(this.description, that.description) &&
            Objects.equals(this.explicitMappings, that.explicitMappings) &&
            Objects.equals(this.name, that.name) &&
            Objects.equals(this.pcri, that.pcri);
    }

    @Override
    public int hashCode() {
        return Objects.hash(
            this.defaultWorkflow, 
            this.description, 
            this.explicitMappings, 
            this.name, 
            this.pcri
        );
    }

    @Override
    public String toString() {
        return "WorkflowSchemePayload{" +
            "defaultWorkflow=" + defaultWorkflow + ", " + 
            "description=" + description + ", " + 
            "explicitMappings=" + explicitMappings + ", " + 
            "name=" + name + ", " + 
            "pcri=" + pcri +
            "}";
    }
}
