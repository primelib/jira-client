// WARNING: This file was generated by PrimeCodeGen. DO NOT EDIT.

package io.github.primelib.jira4j.rest.v2.spring;

import io.github.primelib.jira4j.rest.v2.JiraClientFactory;
import io.github.primelib.jira4j.rest.v2.client.JiraClientAnnouncementBannerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientAnnouncementBannerConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientAppDataPoliciesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientAppDataPoliciesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientAppMigrationApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientAppMigrationConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientAppPropertiesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientAppPropertiesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientApplicationRolesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientApplicationRolesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientAuditRecordsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientAuditRecordsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientAvatarsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientAvatarsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientClassificationLevelsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientClassificationLevelsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientDashboardsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientDashboardsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientDynamicModulesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientDynamicModulesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientFilterSharingApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientFilterSharingConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientFiltersApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientFiltersConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientGroupAndUserPickerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientGroupAndUserPickerConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientGroupsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientGroupsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueAttachmentsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueAttachmentsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueCommentPropertiesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueCommentPropertiesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueCommentsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueCommentsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueCustomFieldAssociationsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueCustomFieldAssociationsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueCustomFieldConfigurationAppsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueCustomFieldConfigurationAppsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueCustomFieldContextsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueCustomFieldContextsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueCustomFieldOptionsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueCustomFieldOptionsAppsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueCustomFieldOptionsAppsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueCustomFieldOptionsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueCustomFieldValuesAppsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueCustomFieldValuesAppsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueFieldConfigurationsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueFieldConfigurationsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueFieldsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueFieldsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueLinkTypesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueLinkTypesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueLinksApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueLinksConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueNavigatorSettingsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueNavigatorSettingsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueNotificationSchemesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueNotificationSchemesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssuePrioritiesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssuePrioritiesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssuePropertiesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssuePropertiesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueRedactionApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueRedactionConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueRemoteLinksApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueRemoteLinksConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueResolutionsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueResolutionsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueSearchApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueSearchConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueSecurityLevelApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueSecurityLevelConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueSecuritySchemeApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueSecuritySchemeConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueSecuritySchemesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueSecuritySchemesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueTypePropertiesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueTypePropertiesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueTypeSchemesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueTypeSchemesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueTypeScreenSchemesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueTypeScreenSchemesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueTypesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueTypesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueVotesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueVotesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueWatchersApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueWatchersConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueWorklogPropertiesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueWorklogPropertiesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueWorklogsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssueWorklogsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssuesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientIssuesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientJiraExpressionsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientJiraExpressionsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientJiraSettingsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientJiraSettingsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientJqlApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientJqlConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientJqlFunctionsAppsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientJqlFunctionsAppsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientLabelsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientLabelsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientLicenseMetricsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientLicenseMetricsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientMyselfApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientMyselfConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientPermissionSchemesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientPermissionSchemesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientPermissionsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientPermissionsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientPlansApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientPlansConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientPrioritySchemesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientPrioritySchemesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectAvatarsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectAvatarsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectCategoriesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectCategoriesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectClassificationLevelsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectClassificationLevelsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectComponentsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectComponentsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectEmailApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectEmailConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectFeaturesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectFeaturesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectKeyAndNameValIDAtionApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectKeyAndNameValIDAtionConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectPermissionSchemesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectPermissionSchemesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectPropertiesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectPropertiesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectRoleActorsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectRoleActorsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectRolesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectRolesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectTemplatesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectTemplatesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectTypesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectTypesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectVersionsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectVersionsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientProjectsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientScreenSchemesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientScreenSchemesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientScreenTabFieldsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientScreenTabFieldsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientScreenTabsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientScreenTabsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientScreensApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientScreensConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientServerInfoApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientServerInfoConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientServiceRegistryApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientServiceRegistryConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientStatusApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientStatusConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientTasksApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientTasksConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientTeamsInPlanApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientTeamsInPlanConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientTimeTrackingApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientTimeTrackingConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientUiModificationsAppsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientUiModificationsAppsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientUserPropertiesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientUserPropertiesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientUserSearchApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientUserSearchConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientUsersApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientUsersConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWebhooksApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWebhooksConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWorkflowSchemeDraftsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWorkflowSchemeDraftsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWorkflowSchemeProjectAssociationsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWorkflowSchemeProjectAssociationsConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWorkflowSchemesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWorkflowSchemesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWorkflowStatusCategoriesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWorkflowStatusCategoriesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWorkflowStatusesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWorkflowStatusesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWorkflowTransitionPropertiesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWorkflowTransitionPropertiesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWorkflowTransitionRulesApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWorkflowTransitionRulesConsumerApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWorkflowsApi;
import io.github.primelib.jira4j.rest.v2.client.JiraClientWorkflowsConsumerApi;
import io.github.primelib.primecodegenlib.java.feign.common.api.AuthMethod;
import io.github.primelib.primecodegenlib.java.feign.common.auth.BearerAuthSpec;
import io.github.primelib.primecodegenlib.java.feign.common.auth.OAuth2ClientCredentialsAuthSpec;
import io.github.primelib.primecodegenlib.java.feign.common.auth.OAuth2UserCredentialsAuthSpec;
import io.micrometer.core.instrument.MeterRegistry;
import java.util.List;
import javax.annotation.processing.Generated;
import lombok.Data;
import lombok.extern.slf4j.Slf4j;
import org.springframework.boot.autoconfigure.AutoConfiguration;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.boot.context.properties.EnableConfigurationProperties;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Primary;

@AutoConfiguration
@EnableConfigurationProperties(JiraClientSpringAutoConfiguration.Properties.class)
@ConditionalOnProperty(name = "jira-client.url", matchIfMissing = false)
@Slf4j
@Generated(value = "io.github.primelib.primecodegen")
public class JiraClientSpringAutoConfiguration {
    private final Properties properties;
    private final MeterRegistry meterRegistry;

    public JiraClientSpringAutoConfiguration(Properties properties, MeterRegistry meterRegistry) {
        this.properties = properties;
        this.meterRegistry = meterRegistry;
    }

    @Bean
    @Primary
    public JiraClientApi JiraClientApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientConsumerApi JiraClientConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    
    @Bean
    @Primary
    public JiraClientAnnouncementBannerApi JiraClientAnnouncementBannerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientAnnouncementBannerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientAnnouncementBannerConsumerApi JiraClientAnnouncementBannerConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientAnnouncementBannerConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientAppDataPoliciesApi JiraClientAppDataPoliciesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientAppDataPoliciesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientAppDataPoliciesConsumerApi JiraClientAppDataPoliciesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientAppDataPoliciesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientAppMigrationApi JiraClientAppMigrationApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientAppMigrationApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientAppMigrationConsumerApi JiraClientAppMigrationConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientAppMigrationConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientAppPropertiesApi JiraClientAppPropertiesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientAppPropertiesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientAppPropertiesConsumerApi JiraClientAppPropertiesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientAppPropertiesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientApplicationRolesApi JiraClientApplicationRolesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientApplicationRolesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientApplicationRolesConsumerApi JiraClientApplicationRolesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientApplicationRolesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientAuditRecordsApi JiraClientAuditRecordsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientAuditRecordsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientAuditRecordsConsumerApi JiraClientAuditRecordsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientAuditRecordsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientAvatarsApi JiraClientAvatarsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientAvatarsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientAvatarsConsumerApi JiraClientAvatarsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientAvatarsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientClassificationLevelsApi JiraClientClassificationLevelsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientClassificationLevelsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientClassificationLevelsConsumerApi JiraClientClassificationLevelsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientClassificationLevelsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientDashboardsApi JiraClientDashboardsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientDashboardsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientDashboardsConsumerApi JiraClientDashboardsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientDashboardsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientDynamicModulesApi JiraClientDynamicModulesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientDynamicModulesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientDynamicModulesConsumerApi JiraClientDynamicModulesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientDynamicModulesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientFilterSharingApi JiraClientFilterSharingApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientFilterSharingApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientFilterSharingConsumerApi JiraClientFilterSharingConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientFilterSharingConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientFiltersApi JiraClientFiltersApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientFiltersApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientFiltersConsumerApi JiraClientFiltersConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientFiltersConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientGroupAndUserPickerApi JiraClientGroupAndUserPickerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientGroupAndUserPickerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientGroupAndUserPickerConsumerApi JiraClientGroupAndUserPickerConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientGroupAndUserPickerConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientGroupsApi JiraClientGroupsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientGroupsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientGroupsConsumerApi JiraClientGroupsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientGroupsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueAttachmentsApi JiraClientIssueAttachmentsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueAttachmentsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueAttachmentsConsumerApi JiraClientIssueAttachmentsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueAttachmentsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueCommentPropertiesApi JiraClientIssueCommentPropertiesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueCommentPropertiesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueCommentPropertiesConsumerApi JiraClientIssueCommentPropertiesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueCommentPropertiesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueCommentsApi JiraClientIssueCommentsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueCommentsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueCommentsConsumerApi JiraClientIssueCommentsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueCommentsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueCustomFieldAssociationsApi JiraClientIssueCustomFieldAssociationsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueCustomFieldAssociationsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueCustomFieldAssociationsConsumerApi JiraClientIssueCustomFieldAssociationsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueCustomFieldAssociationsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueCustomFieldConfigurationAppsApi JiraClientIssueCustomFieldConfigurationAppsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueCustomFieldConfigurationAppsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueCustomFieldConfigurationAppsConsumerApi JiraClientIssueCustomFieldConfigurationAppsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueCustomFieldConfigurationAppsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueCustomFieldContextsApi JiraClientIssueCustomFieldContextsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueCustomFieldContextsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueCustomFieldContextsConsumerApi JiraClientIssueCustomFieldContextsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueCustomFieldContextsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueCustomFieldOptionsApi JiraClientIssueCustomFieldOptionsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueCustomFieldOptionsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueCustomFieldOptionsConsumerApi JiraClientIssueCustomFieldOptionsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueCustomFieldOptionsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueCustomFieldOptionsAppsApi JiraClientIssueCustomFieldOptionsAppsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueCustomFieldOptionsAppsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueCustomFieldOptionsAppsConsumerApi JiraClientIssueCustomFieldOptionsAppsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueCustomFieldOptionsAppsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueCustomFieldValuesAppsApi JiraClientIssueCustomFieldValuesAppsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueCustomFieldValuesAppsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueCustomFieldValuesAppsConsumerApi JiraClientIssueCustomFieldValuesAppsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueCustomFieldValuesAppsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueFieldConfigurationsApi JiraClientIssueFieldConfigurationsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueFieldConfigurationsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueFieldConfigurationsConsumerApi JiraClientIssueFieldConfigurationsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueFieldConfigurationsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueFieldsApi JiraClientIssueFieldsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueFieldsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueFieldsConsumerApi JiraClientIssueFieldsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueFieldsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueLinkTypesApi JiraClientIssueLinkTypesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueLinkTypesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueLinkTypesConsumerApi JiraClientIssueLinkTypesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueLinkTypesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueLinksApi JiraClientIssueLinksApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueLinksApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueLinksConsumerApi JiraClientIssueLinksConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueLinksConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueNavigatorSettingsApi JiraClientIssueNavigatorSettingsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueNavigatorSettingsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueNavigatorSettingsConsumerApi JiraClientIssueNavigatorSettingsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueNavigatorSettingsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueNotificationSchemesApi JiraClientIssueNotificationSchemesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueNotificationSchemesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueNotificationSchemesConsumerApi JiraClientIssueNotificationSchemesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueNotificationSchemesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssuePrioritiesApi JiraClientIssuePrioritiesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssuePrioritiesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssuePrioritiesConsumerApi JiraClientIssuePrioritiesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssuePrioritiesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssuePropertiesApi JiraClientIssuePropertiesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssuePropertiesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssuePropertiesConsumerApi JiraClientIssuePropertiesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssuePropertiesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueRedactionApi JiraClientIssueRedactionApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueRedactionApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueRedactionConsumerApi JiraClientIssueRedactionConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueRedactionConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueRemoteLinksApi JiraClientIssueRemoteLinksApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueRemoteLinksApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueRemoteLinksConsumerApi JiraClientIssueRemoteLinksConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueRemoteLinksConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueResolutionsApi JiraClientIssueResolutionsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueResolutionsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueResolutionsConsumerApi JiraClientIssueResolutionsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueResolutionsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueSearchApi JiraClientIssueSearchApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueSearchApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueSearchConsumerApi JiraClientIssueSearchConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueSearchConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueSecurityLevelApi JiraClientIssueSecurityLevelApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueSecurityLevelApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueSecurityLevelConsumerApi JiraClientIssueSecurityLevelConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueSecurityLevelConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueSecuritySchemeApi JiraClientIssueSecuritySchemeApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueSecuritySchemeApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueSecuritySchemeConsumerApi JiraClientIssueSecuritySchemeConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueSecuritySchemeConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueSecuritySchemesApi JiraClientIssueSecuritySchemesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueSecuritySchemesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueSecuritySchemesConsumerApi JiraClientIssueSecuritySchemesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueSecuritySchemesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueTypePropertiesApi JiraClientIssueTypePropertiesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueTypePropertiesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueTypePropertiesConsumerApi JiraClientIssueTypePropertiesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueTypePropertiesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueTypeSchemesApi JiraClientIssueTypeSchemesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueTypeSchemesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueTypeSchemesConsumerApi JiraClientIssueTypeSchemesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueTypeSchemesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueTypeScreenSchemesApi JiraClientIssueTypeScreenSchemesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueTypeScreenSchemesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueTypeScreenSchemesConsumerApi JiraClientIssueTypeScreenSchemesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueTypeScreenSchemesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueTypesApi JiraClientIssueTypesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueTypesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueTypesConsumerApi JiraClientIssueTypesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueTypesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueVotesApi JiraClientIssueVotesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueVotesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueVotesConsumerApi JiraClientIssueVotesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueVotesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueWatchersApi JiraClientIssueWatchersApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueWatchersApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueWatchersConsumerApi JiraClientIssueWatchersConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueWatchersConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueWorklogPropertiesApi JiraClientIssueWorklogPropertiesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueWorklogPropertiesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueWorklogPropertiesConsumerApi JiraClientIssueWorklogPropertiesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueWorklogPropertiesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssueWorklogsApi JiraClientIssueWorklogsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueWorklogsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssueWorklogsConsumerApi JiraClientIssueWorklogsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssueWorklogsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientIssuesApi JiraClientIssuesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssuesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientIssuesConsumerApi JiraClientIssuesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientIssuesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientJqlApi JiraClientJqlApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientJqlApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientJqlConsumerApi JiraClientJqlConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientJqlConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientJqlFunctionsAppsApi JiraClientJqlFunctionsAppsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientJqlFunctionsAppsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientJqlFunctionsAppsConsumerApi JiraClientJqlFunctionsAppsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientJqlFunctionsAppsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientJiraExpressionsApi JiraClientJiraExpressionsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientJiraExpressionsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientJiraExpressionsConsumerApi JiraClientJiraExpressionsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientJiraExpressionsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientJiraSettingsApi JiraClientJiraSettingsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientJiraSettingsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientJiraSettingsConsumerApi JiraClientJiraSettingsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientJiraSettingsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientLabelsApi JiraClientLabelsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientLabelsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientLabelsConsumerApi JiraClientLabelsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientLabelsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientLicenseMetricsApi JiraClientLicenseMetricsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientLicenseMetricsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientLicenseMetricsConsumerApi JiraClientLicenseMetricsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientLicenseMetricsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientMyselfApi JiraClientMyselfApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientMyselfApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientMyselfConsumerApi JiraClientMyselfConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientMyselfConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientPermissionSchemesApi JiraClientPermissionSchemesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientPermissionSchemesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientPermissionSchemesConsumerApi JiraClientPermissionSchemesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientPermissionSchemesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientPermissionsApi JiraClientPermissionsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientPermissionsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientPermissionsConsumerApi JiraClientPermissionsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientPermissionsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientPlansApi JiraClientPlansApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientPlansApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientPlansConsumerApi JiraClientPlansConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientPlansConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientPrioritySchemesApi JiraClientPrioritySchemesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientPrioritySchemesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientPrioritySchemesConsumerApi JiraClientPrioritySchemesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientPrioritySchemesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientProjectAvatarsApi JiraClientProjectAvatarsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectAvatarsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientProjectAvatarsConsumerApi JiraClientProjectAvatarsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectAvatarsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientProjectCategoriesApi JiraClientProjectCategoriesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectCategoriesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientProjectCategoriesConsumerApi JiraClientProjectCategoriesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectCategoriesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientProjectClassificationLevelsApi JiraClientProjectClassificationLevelsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectClassificationLevelsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientProjectClassificationLevelsConsumerApi JiraClientProjectClassificationLevelsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectClassificationLevelsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientProjectComponentsApi JiraClientProjectComponentsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectComponentsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientProjectComponentsConsumerApi JiraClientProjectComponentsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectComponentsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientProjectEmailApi JiraClientProjectEmailApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectEmailApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientProjectEmailConsumerApi JiraClientProjectEmailConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectEmailConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientProjectFeaturesApi JiraClientProjectFeaturesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectFeaturesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientProjectFeaturesConsumerApi JiraClientProjectFeaturesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectFeaturesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientProjectKeyAndNameValIDAtionApi JiraClientProjectKeyAndNameValIDAtionApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectKeyAndNameValIDAtionApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientProjectKeyAndNameValIDAtionConsumerApi JiraClientProjectKeyAndNameValIDAtionConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectKeyAndNameValIDAtionConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientProjectPermissionSchemesApi JiraClientProjectPermissionSchemesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectPermissionSchemesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientProjectPermissionSchemesConsumerApi JiraClientProjectPermissionSchemesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectPermissionSchemesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientProjectPropertiesApi JiraClientProjectPropertiesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectPropertiesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientProjectPropertiesConsumerApi JiraClientProjectPropertiesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectPropertiesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientProjectRoleActorsApi JiraClientProjectRoleActorsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectRoleActorsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientProjectRoleActorsConsumerApi JiraClientProjectRoleActorsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectRoleActorsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientProjectRolesApi JiraClientProjectRolesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectRolesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientProjectRolesConsumerApi JiraClientProjectRolesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectRolesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientProjectTemplatesApi JiraClientProjectTemplatesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectTemplatesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientProjectTemplatesConsumerApi JiraClientProjectTemplatesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectTemplatesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientProjectTypesApi JiraClientProjectTypesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectTypesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientProjectTypesConsumerApi JiraClientProjectTypesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectTypesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientProjectVersionsApi JiraClientProjectVersionsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectVersionsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientProjectVersionsConsumerApi JiraClientProjectVersionsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectVersionsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientProjectsApi JiraClientProjectsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientProjectsConsumerApi JiraClientProjectsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientProjectsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientScreenSchemesApi JiraClientScreenSchemesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientScreenSchemesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientScreenSchemesConsumerApi JiraClientScreenSchemesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientScreenSchemesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientScreenTabFieldsApi JiraClientScreenTabFieldsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientScreenTabFieldsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientScreenTabFieldsConsumerApi JiraClientScreenTabFieldsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientScreenTabFieldsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientScreenTabsApi JiraClientScreenTabsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientScreenTabsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientScreenTabsConsumerApi JiraClientScreenTabsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientScreenTabsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientScreensApi JiraClientScreensApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientScreensApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientScreensConsumerApi JiraClientScreensConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientScreensConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientServerInfoApi JiraClientServerInfoApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientServerInfoApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientServerInfoConsumerApi JiraClientServerInfoConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientServerInfoConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientServiceRegistryApi JiraClientServiceRegistryApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientServiceRegistryApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientServiceRegistryConsumerApi JiraClientServiceRegistryConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientServiceRegistryConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientStatusApi JiraClientStatusApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientStatusApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientStatusConsumerApi JiraClientStatusConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientStatusConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientTasksApi JiraClientTasksApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientTasksApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientTasksConsumerApi JiraClientTasksConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientTasksConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientTeamsInPlanApi JiraClientTeamsInPlanApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientTeamsInPlanApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientTeamsInPlanConsumerApi JiraClientTeamsInPlanConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientTeamsInPlanConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientTimeTrackingApi JiraClientTimeTrackingApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientTimeTrackingApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientTimeTrackingConsumerApi JiraClientTimeTrackingConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientTimeTrackingConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientUiModificationsAppsApi JiraClientUiModificationsAppsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientUiModificationsAppsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientUiModificationsAppsConsumerApi JiraClientUiModificationsAppsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientUiModificationsAppsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientUserPropertiesApi JiraClientUserPropertiesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientUserPropertiesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientUserPropertiesConsumerApi JiraClientUserPropertiesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientUserPropertiesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientUserSearchApi JiraClientUserSearchApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientUserSearchApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientUserSearchConsumerApi JiraClientUserSearchConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientUserSearchConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientUsersApi JiraClientUsersApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientUsersApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientUsersConsumerApi JiraClientUsersConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientUsersConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientWebhooksApi JiraClientWebhooksApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWebhooksApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientWebhooksConsumerApi JiraClientWebhooksConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWebhooksConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientWorkflowSchemeDraftsApi JiraClientWorkflowSchemeDraftsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWorkflowSchemeDraftsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientWorkflowSchemeDraftsConsumerApi JiraClientWorkflowSchemeDraftsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWorkflowSchemeDraftsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientWorkflowSchemeProjectAssociationsApi JiraClientWorkflowSchemeProjectAssociationsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWorkflowSchemeProjectAssociationsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientWorkflowSchemeProjectAssociationsConsumerApi JiraClientWorkflowSchemeProjectAssociationsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWorkflowSchemeProjectAssociationsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientWorkflowSchemesApi JiraClientWorkflowSchemesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWorkflowSchemesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientWorkflowSchemesConsumerApi JiraClientWorkflowSchemesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWorkflowSchemesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientWorkflowStatusCategoriesApi JiraClientWorkflowStatusCategoriesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWorkflowStatusCategoriesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientWorkflowStatusCategoriesConsumerApi JiraClientWorkflowStatusCategoriesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWorkflowStatusCategoriesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientWorkflowStatusesApi JiraClientWorkflowStatusesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWorkflowStatusesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientWorkflowStatusesConsumerApi JiraClientWorkflowStatusesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWorkflowStatusesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientWorkflowTransitionPropertiesApi JiraClientWorkflowTransitionPropertiesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWorkflowTransitionPropertiesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientWorkflowTransitionPropertiesConsumerApi JiraClientWorkflowTransitionPropertiesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWorkflowTransitionPropertiesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientWorkflowTransitionRulesApi JiraClientWorkflowTransitionRulesApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWorkflowTransitionRulesApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientWorkflowTransitionRulesConsumerApi JiraClientWorkflowTransitionRulesConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWorkflowTransitionRulesConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    
    @Bean
    @Primary
    public JiraClientWorkflowsApi JiraClientWorkflowsApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWorkflowsApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }

    @Bean
    @Primary
    public JiraClientWorkflowsConsumerApi JiraClientWorkflowsConsumerApi() {
        return JiraClientFactory.create(spec -> {
            spec.api(JiraClientWorkflowsConsumerApi.class);
            spec.baseUrl(properties.getUrl());
            spec.auth(buildAuthSpecs(properties));
            if (properties.getInsecure() != null) {
                spec.insecure(properties.getInsecure());
            }
            if (properties.getBackendName() != null && !properties.getBackendName().isEmpty()) {
                spec.backendName(properties.getBackendName());
            }
            if (properties.getLogLevel() != null && !properties.getLogLevel().isEmpty()) {
                spec.logLevel(properties.getLogLevel());
            }
            spec.meterRegistry(meterRegistry);
        });
    }
    

    private List<AuthMethod> buildAuthSpecs(Properties props) {
        if (props.getAuth() == null || props.getAuth().getType() == null) {
            return null;
        }

        switch (props.getAuth().getType()) {
            case "oauth2-user":
                return List.of(new OAuth2UserCredentialsAuthSpec(auth -> {
                    auth.tokenEndpoint(props.getAuth().getTokenEndpoint());
                    auth.clientId(props.getAuth().getClientId());
                    auth.clientSecret(props.getAuth().getClientSecret());
                    auth.username(props.getAuth().getUsername());
                    auth.password(props.getAuth().getPassword());
                }));
            case "oauth2-client":
                return List.of(new OAuth2ClientCredentialsAuthSpec(auth -> {
                    auth.tokenEndpoint(props.getAuth().getTokenEndpoint());
                    auth.clientId(props.getAuth().getClientId());
                    auth.clientSecret(props.getAuth().getClientSecret());
                }));
            case "bearer":
                return List.of(new BearerAuthSpec(auth -> {
                    if (props.getAuth().getTokenPropertyKey() != null && !props.getAuth().getTokenPropertyKey().isEmpty()) {
                        auth.propertyKey(props.getAuth().getTokenPropertyKey());
                    }
                    if (props.getAuth().getTokenValueTemplate() != null && !props.getAuth().getTokenValueTemplate().isEmpty()) {
                        auth.propertyKey(props.getAuth().getTokenValueTemplate());
                    }
                    auth.propertyKey(props.getAuth().getToken());
                }));
            default:
                return null;
        }
    }

    @Data
    @ConfigurationProperties(prefix = "jira-client")
    public static class Properties {
        private String url = "";
        private String backendName = "";
        private String logLevel = "";
        private Boolean insecure = false;
        private Auth auth;

        @Data
        public static class Auth {
            /**
             * Type of authentication.
             * Supported values: "oauth2-user", "oauth2-client", "bearer".
             */
            private String type;
            /**
             * Full token endpoint URL
             */
            private String tokenEndpoint;
            private String clientId;
            private String clientSecret;
            private String username;
            private String password;
            private String token;
            /**
             * Header key to pass the token in (used by BearerAuthSpec).
             */
            private String tokenPropertyKey;
            /**
             * Template to generate token value (used by BearerAuthSpec).
             */
            private String tokenValueTemplate;
        }
    }
}
